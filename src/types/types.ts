export type UserT = {
    id: number
    userName: string
    color: string
    type: string
    status: boolean
}

export type Option = {
    name: string, 
    value: string,
    disabled?: boolean
}